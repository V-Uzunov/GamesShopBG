@model ShoppingCartServiceModel

@{
    ViewBag.Title = "You Shopping Cart";
}

<h2>@ViewBag.Title</h2>
<table class="table table-bordered table-striped">
    <thead>
        <tr>
            <th>Selected amount</th>
            <th>Game</th>
            <th class="text-right">Price</th>
            <th class="text-right">Subtotal</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var line in Model.ShoppingCart.ShoppingCartItems)
        {
            <tr>
                <td class="text-center">@line.Amount</td>
                <td class="text-left">@line.Game.Title</td>
                <td class="text-right">@line.Game.Price.ToString("c")</td>
                <td class="text-right">
                    @((line.Amount * line.Game.Price).ToString("c"))
                </td>
            </tr>
        }
    </tbody>
    <tfoot>
        <tr>
            <td colspan="3" class="text-right">Total:</td>
            <td class="text-right">
                @Model.ShoppingCartTotal.ToString("c")
            </td>
        </tr>
    </tfoot>
</table>

@*<div progress-value="33">
    </div>*@

<custom:div progress-value="33">
</custom:div>

<div class="text-center">
    @Html.ActionLink("Check out now!", "Checkout", "Order", new { @class="btn btn-primary"})
</div>

